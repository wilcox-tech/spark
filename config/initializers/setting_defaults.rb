# The default names (primarily used by the unit tests)
Settings.defaults[:instance_name] = 'Spark'
Settings.defaults[:org_name] = 'Your Name Here'

# Whether or not we want database auth.
Settings.defaults[:db_auth] = true
